# --- API service (required to run `uvicorn src.api:app`) -------------------
fastapi>=0.103.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6   # telemetry file uploads
pandas>=2.0.0
numpy>=1.24.0
reportlab>=4.0.0          # PDF audit evidence report
requests>=2.31.0          # MITRE / NVD / vendor connectors

# --- Data generation, ML pipeline and offline analysis scripts ------------
# Needed by src/train_models.py, src/feature_engineering.py,
# src/explainability.py and src/risk_engine.py. The API itself does not
# import these, so the service can run without them installed.
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
shap>=0.42.0
scipy>=1.11.0
statsmodels>=0.14.0
joblib>=1.3.0
mlflow>=2.7.0
streamlit>=1.26.0
plotly>=5.16.0
transformers
peft
trl
bitsandbytes
datasets
markdown
xhtml2pdf

# --- Legacy standalone optimiser -----------------------------------------
# src/investment_optimizer.py solves the knapsack with pulp. The API uses a
# dependency-free dynamic-programming knapsack instead
# (src/services/optimizer.py), per PS26105_Backend_Only_Prompt.md 4.5.
pulp>=2.7.0
